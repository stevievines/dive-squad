$('#diver-<%= @diver.id %>-goals').html("<%= escape_javascript(render 'divers/goals', diver: @diver) %>");

$('.js-dive-select').select2({
  placeholder: "Choose a dive",
  allowClear: true,
  width: '100%'
});
