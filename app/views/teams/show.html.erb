<h1><%= @team.name %></h1>

Coaches:
<ul>
  <% @team.coaches.each do |coach| %>
    <li><%= link_to(coach.name, coach_path(coach)) %></li>
  <% end %>
</ul>

Divers:
<ul>
  <% @team.divers.each do |diver| %>
    <li><%= link_to(diver.name, diver_path(diver)) %></li>
  <% end %>
</ul>
<%= link_to('Add a Diver', new_diver_path) %>
