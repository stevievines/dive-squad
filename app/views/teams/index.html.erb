<h1>Your Teams!</h1>
<ul>
  <% @teams.each do |team| %>
    <li><%= link_to(team.name, team_path(team)) %></li>
  <% end %>
</ul>
<%= link_to('Add a Team', new_team_path) %>
<%= link_to('Add a Diver', new_diver_path) %>
